apiVersion: v1
kind: Service
metadata:
  name: frostyyeti-crowdfund-service
  namespace: frostyyeti-namespace
spec:
  selector:
    app: frostyyeti-crowdfund
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer